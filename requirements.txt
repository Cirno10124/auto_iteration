# Python version: 3.11.13
# 项目依赖（固定版本以匹配 lora_env.txt）
torch==2.8.0
transformers==4.56.1
datasets==4.0.0
accelerate==1.10.1
peft==0.5.0
numpy==2.3.3
scipy==1.16.2
sounddevice==0.4.0
torchaudio>=2.6.0,<2.10.0  # 保留, 与 Python 环境兼容
# 添加说话人分离依赖
pyannote.audio==4.0.3  # 与 Python3.11+ 兼容版本

soundfile>=0.13.1,<0.15  # 与 pyannote.audio 4.x 兼容版本
webrtcvad==2.0.10
jiwer==4.0.0
evaluate==0.4.5
tqdm==4.67.1

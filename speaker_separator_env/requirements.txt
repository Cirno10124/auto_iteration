# 最小化依赖：仅覆盖 auto_iteration/speaker_separator.py 的运行所需
# Python: 3.11.x
#
# 注意：
# - torch/torchaudio 需要根据你的平台（CPU/CUDA/ROCm）选择正确的 wheel。
# - 若你已经在服务器上单独安装了 torch/torchaudio，可保留本文件版本或按需调整。

numpy==2.3.3
soundfile==0.13.1
scikit-learn==1.7.2

# 说话人分离 + embedding（会拉起一系列 pyannote 依赖）
pyannote-audio==4.0.1

# 预处理/降噪
librosa>=0.10.2.post1
noisereduce>=3.0.2

# 深度学习运行时
torch==2.9.0
torchaudio==2.9.0



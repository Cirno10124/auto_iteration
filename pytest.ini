[pytest]
python_files = test_*.py *_test.py
markers =
    unit: pure logic unit tests without model or network dependencies
    integration_light: lightweight integration tests with stubs, CI-safe
    heavy: heavy tests requiring real model, network, GPU, or large datasets
